const express = require('express');
const router = express.Router();
const { findUserID, addToWishlist, deleteFromWishlist } = require('../controllers/wishlistCtrl');

// ROUTES


module.exports = router;